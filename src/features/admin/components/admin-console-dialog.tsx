"use client";

import { Bot, Shield, Trash2, Users, Webhook } from "lucide-react";
import { useCallback, useEffect, useState } from "react";
import { Button } from "@/shared/ui/button";
import { Dialog, DialogContent, DialogDescription, DialogHeader, DialogTitle } from "@/shared/ui/dialog";
import { Input } from "@/shared/ui/input";
import { Label } from "@/shared/ui/label";
import type { Board, WorkspaceMembership, WorkspaceRole } from "@/features/workspaces/types";
import * as api from "@/features/workspaces/client/api";
import { AgentsDialog } from "@/features/agents/components/agents-dialog";
import { WebhooksDialog } from "@/features/webhooks/components/webhooks-dialog";
import { MembersDialog } from "@/features/workspaces/components/members-dialog";
import * as cfApi from "@/features/custom-fields/client/api";
import type { CustomField } from "@/features/custom-fields/types";
import type { AdminSummary, AuditEvent, BoardIntakeAddress, CustomFieldPolicy, IpAllowlistEntry, PermissionGrant } from "../types";
import { EnterpriseControls } from "./enterprise-controls";
import { Select, SelectItem } from "@/shared/ui/select";

export function AdminConsoleButton({ workspace, currentUserId, boards }: { workspace: WorkspaceMembership; currentUserId: string; boards: Board[] }) {
  if (workspace.role !== "owner" && workspace.role !== "admin") return null;
  return <AdminConsoleControl workspace={workspace} currentUserId={currentUserId} boards={boards} />;
}

function AdminConsoleControl({ workspace, currentUserId, boards }: { workspace: WorkspaceMembership; currentUserId: string; boards: Board[] }) {
  const [open, setOpen] = useState(false);
  return <><Button variant="ghost" size="icon" aria-label="Admin console" title="Admin console" onClick={() => setOpen(true)}><Shield /></Button><AdminConsoleDialog workspace={workspace} currentUserId={currentUserId} boards={boards} open={open} onOpenChange={setOpen} /></>;
}

/** Exported so the Settings surface can host it as a section (it renders inline
 *  there); the sidebar button above still opens it as its own dialog. */
export function AdminConsoleDialog({ workspace, currentUserId, boards, open, onOpenChange }: { workspace: WorkspaceMembership; currentUserId: string; boards: Board[]; open: boolean; onOpenChange(open: boolean): void }) {
  const [entries, setEntries] = useState<IpAllowlistEntry[]>([]);
  const [summary, setSummary] = useState<AdminSummary | null>(null);
  const [grants, setGrants] = useState<PermissionGrant[]>([]);
  const [grantBoardId, setGrantBoardId] = useState(""); const [grantRole, setGrantRole] = useState<WorkspaceRole>("guest"); const [grantCapability, setGrantCapability] = useState<"read" | "write" | "manage">("read");
  const [cidr, setCidr] = useState(""); const [label, setLabel] = useState("");
  const [error, setError] = useState<string | null>(null); const [busy, setBusy] = useState(false);
  const [membersOpen, setMembersOpen] = useState(false); const [agentsOpen, setAgentsOpen] = useState(false); const [webhooksOpen, setWebhooksOpen] = useState(false);
  // Custom-field access policies (per-field, per-role visibility).
  const [fieldPolicies, setFieldPolicies] = useState<CustomFieldPolicy[]>([]);
  const [policyBoardId, setPolicyBoardId] = useState(""); const [boardFields, setBoardFields] = useState<CustomField[]>([]);
  const [policyFieldId, setPolicyFieldId] = useState(""); const [policyRole, setPolicyRole] = useState<WorkspaceRole>("guest"); const [policyAccess, setPolicyAccess] = useState<"hidden" | "view" | "edit">("view");
  // Email intake (per-board inbound addresses), shown only when configured.
  const [intake, setIntake] = useState<{ configured: boolean; addresses: BoardIntakeAddress[] } | null>(null);
  const [copiedBoardId, setCopiedBoardId] = useState<number | null>(null);
  // Audit-log viewer: one page of recent workspace-wide events.
  const AUDIT_PAGE = 25;
  const [audit, setAudit] = useState<AuditEvent[]>([]); const [auditOffset, setAuditOffset] = useState(0);
  const load = useCallback(async () => { try { const [nextSummary, nextEntries, nextGrants, nextPolicies, nextIntake] = await Promise.all([api.fetchAdminSummary(workspace.id), api.fetchIpAllowlist(workspace.id), api.fetchBoardGrants(workspace.id), api.fetchFieldPolicies(workspace.id), api.fetchEmailIntake(workspace.id)]); setSummary(nextSummary); setEntries(nextEntries); setGrants(nextGrants); setFieldPolicies(nextPolicies); setIntake(nextIntake); } catch (e) { setError(e instanceof Error ? e.message : "Could not load admin settings"); } }, [workspace.id]);
  const loadAudit = useCallback(async (offset: number) => { try { setAudit(await api.fetchAuditLog(workspace.id, AUDIT_PAGE, offset)); setAuditOffset(offset); } catch (e) { setError(e instanceof Error ? e.message : "Could not load the audit log"); } }, [workspace.id]);
  // The field picker follows the chosen board; fields load lazily per pick.
  useEffect(() => {
    if (!policyBoardId) { setBoardFields([]); setPolicyFieldId(""); return; }
    let cancelled = false;
    void cfApi.fetchBoardFields(Number(policyBoardId)).then((fields) => { if (!cancelled) { setBoardFields(fields); setPolicyFieldId(""); } }).catch(() => { if (!cancelled) setBoardFields([]); });
    return () => { cancelled = true; };
  }, [policyBoardId]);
  useEffect(() => {
    if (!open) return;
    // Defer the request until after the dialog has committed. This avoids a
    // render-phase update while still refreshing on every open.
    const timer = setTimeout(() => { void load(); void loadAudit(0); }, 0);
    return () => clearTimeout(timer);
  }, [open, load, loadAudit]);
  async function add() { setBusy(true); setError(null); try { await api.addIpAllowlistEntry(workspace.id, cidr, label); setCidr(""); setLabel(""); await load(); } catch (e) { setError(e instanceof Error ? e.message : "Could not save network policy"); } finally { setBusy(false); } }
  async function remove(id: number) { setBusy(true); setError(null); try { await api.deleteIpAllowlistEntry(workspace.id, id); await load(); } catch (e) { setError(e instanceof Error ? e.message : "Could not remove network policy"); } finally { setBusy(false); } }
  async function addGrant() { if (!grantBoardId) return; setBusy(true); setError(null); try { await api.grantBoardPermission(workspace.id, { subjectId: grantBoardId, principalType: "workspace_role", principalId: grantRole, capability: grantCapability }); await load(); } catch (e) { setError(e instanceof Error ? e.message : "Could not save board permission"); } finally { setBusy(false); } }
  async function removeGrant(id: string) { setBusy(true); setError(null); try { await api.revokeBoardGrant(workspace.id, id); await load(); } catch (e) { setError(e instanceof Error ? e.message : "Could not remove board permission"); } finally { setBusy(false); } }
  async function addFieldPolicy() { if (!policyFieldId) return; setBusy(true); setError(null); try { await api.saveFieldPolicy(workspace.id, { fieldId: Number(policyFieldId), role: policyRole, canView: policyAccess !== "hidden", canEdit: policyAccess === "edit" }); await load(); } catch (e) { setError(e instanceof Error ? e.message : "Could not save field access policy"); } finally { setBusy(false); } }
  async function removeFieldPolicy(fieldId: number, role: string) { setBusy(true); setError(null); try { await api.deleteFieldPolicy(workspace.id, fieldId, role); await load(); } catch (e) { setError(e instanceof Error ? e.message : "Could not remove field access policy"); } finally { setBusy(false); } }
  async function copyIntake(address: BoardIntakeAddress) { try { await navigator.clipboard.writeText(address.address); setCopiedBoardId(address.boardId); setTimeout(() => setCopiedBoardId(null), 1500); } catch { setError("Could not copy the address"); } }
  const boardName = new Map(boards.map(b => [b.id, b.name]));
  const owner = workspace.role === "owner";
  return <><Dialog open={open} onOpenChange={onOpenChange}><DialogContent className="max-h-[85vh] overflow-y-auto sm:max-w-2xl"><DialogHeader><DialogTitle>Admin console</DialogTitle><DialogDescription>Central workspace administration for {workspace.name}.</DialogDescription></DialogHeader>{summary && <div className="grid grid-cols-2 gap-2 border-y py-3 text-sm"><p><b>{summary.members}</b> members</p><p><b>{summary.agents}</b> agents</p><p><b>{summary.boards}</b> boards</p><p><b>{summary.webhooks}</b> active webhooks</p><p className="col-span-2"><b>{summary.auditEvents}</b> audit events</p></div>}<div className="flex flex-wrap gap-2"><Button variant="outline" size="sm" onClick={() => setMembersOpen(true)}><Users /> Members</Button><Button variant="outline" size="sm" onClick={() => setAgentsOpen(true)}><Bot /> Agents</Button><Button variant="outline" size="sm" onClick={() => setWebhooksOpen(true)}><Webhook /> Webhooks</Button></div><section className="grid gap-2 border-t pt-3"><h3 className="text-sm font-medium">Board permissions</h3><div className="flex gap-2"><Select aria-label="Board" value={grantBoardId} onValueChange={setGrantBoardId}><SelectItem value="">Choose board</SelectItem>{boards.map(b=><SelectItem key={b.id} value={String(b.id)}>{b.name}</SelectItem>)}</Select><Select aria-label="Workspace role" value={grantRole} onValueChange={value=>setGrantRole(value as WorkspaceRole)}>{["guest","viewer","member","admin","owner"].map(r=><SelectItem key={r} value={r}>{r}</SelectItem>)}</Select><Select aria-label="Capability" value={grantCapability} onValueChange={value=>setGrantCapability(value as "read"|"write"|"manage")}>{["read","write","manage"].map(c=><SelectItem key={c} value={c}>{c}</SelectItem>)}</Select><Button size="sm" disabled={busy||!grantBoardId} onClick={addGrant}>Grant</Button></div>{grants.map(g=><div key={g.id} className="flex gap-2 text-xs"><span className="flex-1">Board #{g.subjectId}: {g.principalId} → {g.capability}</span><Button variant="ghost" size="icon" className="size-6" disabled={busy} aria-label="Revoke board permission" onClick={()=>removeGrant(g.id)}><Trash2 /></Button></div>)}</section><section className="grid gap-2 border-t pt-3"><div><h3 className="text-sm font-medium">Custom field access</h3><p className="text-xs text-muted-foreground">Restrict who sees or edits a board&rsquo;s custom field. Without a policy every member sees the field; owners and admins always do.</p></div><div className="flex flex-wrap gap-2"><Select aria-label="Policy board" value={policyBoardId} onValueChange={setPolicyBoardId}><SelectItem value="">Choose board</SelectItem>{boards.map(b=><SelectItem key={b.id} value={String(b.id)}>{b.name}</SelectItem>)}</Select><Select aria-label="Policy field" value={policyFieldId} onValueChange={setPolicyFieldId}><SelectItem value="">Choose field</SelectItem>{boardFields.map(f=><SelectItem key={f.id} value={String(f.id)}>{f.name}</SelectItem>)}</Select><Select aria-label="Policy role" value={policyRole} onValueChange={value=>setPolicyRole(value as WorkspaceRole)}>{["guest","viewer","member","admin","owner"].map(r=><SelectItem key={r} value={r}>{r}</SelectItem>)}</Select><Select aria-label="Policy access" value={policyAccess} onValueChange={value=>setPolicyAccess(value as "hidden"|"view"|"edit")}><SelectItem value="hidden">hidden</SelectItem><SelectItem value="view">view</SelectItem><SelectItem value="edit">view + edit</SelectItem></Select><Button size="sm" disabled={busy||!policyFieldId} onClick={addFieldPolicy}>Set</Button></div>{fieldPolicies.map(p=><div key={`${p.fieldId}-${p.role}`} className="flex gap-2 text-xs"><span className="flex-1">{boardName.get(p.boardId) ?? `Board #${p.boardId}`} / {p.fieldName}: {p.role} → {p.canEdit ? "view + edit" : p.canView ? "view" : "hidden"}</span><Button variant="ghost" size="icon" className="size-6" disabled={busy} aria-label={`Remove ${p.fieldName} policy for ${p.role}`} onClick={()=>removeFieldPolicy(p.fieldId,p.role)}><Trash2 /></Button></div>)}</section>{intake?.configured && intake.addresses.length > 0 && <section className="grid gap-2 border-t pt-3"><div><h3 className="text-sm font-medium">Email intake</h3><p className="text-xs text-muted-foreground">Send mail to a board&rsquo;s address to create a task; reply to a task notification to comment. Members only, verified by the inbound gateway.</p></div><div className="grid gap-1">{intake.addresses.map(a=><div key={a.boardId} className="flex items-center gap-2 rounded-md border px-2 py-1.5 text-xs"><span className="shrink-0 text-muted-foreground">{a.boardName}</span><code className="min-w-0 flex-1 truncate">{a.address}</code><Button variant="outline" size="sm" className="h-6 px-2 text-xs" onClick={()=>void copyIntake(a)}>{copiedBoardId===a.boardId?"Copied":"Copy"}</Button></div>)}</div></section>}<section className="grid gap-2 border-t pt-3"><div className="flex items-center justify-between"><h3 className="text-sm font-medium">Audit log</h3><span className="flex items-center gap-1"><Button variant="outline" size="sm" className="h-6 px-2 text-xs" disabled={auditOffset===0} onClick={()=>void loadAudit(Math.max(0,auditOffset-AUDIT_PAGE))}>Newer</Button><Button variant="outline" size="sm" className="h-6 px-2 text-xs" disabled={audit.length<AUDIT_PAGE} onClick={()=>void loadAudit(auditOffset+AUDIT_PAGE)}>Older</Button></span></div>{audit.length===0 ? <p className="text-xs text-muted-foreground">No audit events on this page.</p> : <ul className="grid gap-1 text-xs">{audit.map(event=><li key={event.id} className="flex items-baseline gap-2"><time dateTime={event.createdAt} className="shrink-0 text-muted-foreground tabular-nums">{new Date(event.createdAt).toLocaleString()}</time><span className="min-w-0 flex-1 truncate">{event.actorName ?? `${event.actorType} ${event.actorId}`} · {event.action}{event.taskId!=null && ` · task #${event.taskId}`}{event.boardId!=null && ` · ${boardName.get(event.boardId) ?? `board #${event.boardId}`}`}</span></li>)}</ul>}</section><section className="grid gap-3"><div><h3 className="text-sm font-medium">IP allowlist</h3><p className="text-xs text-muted-foreground">Enforcement is active only when the deployment enables it with a trusted proxy header. See enterprise deployment notes.</p></div>{!owner && <p className="text-xs text-muted-foreground">Only workspace owners can change this policy.</p>}{owner && <div className="grid gap-2"><Label htmlFor="allow-cidr">CIDR range</Label><div className="flex gap-2"><Input id="allow-cidr" value={cidr} onChange={(e) => setCidr(e.target.value)} placeholder="203.0.113.0/24" /><Input aria-label="Range label" value={label} onChange={(e) => setLabel(e.target.value)} placeholder="Office (optional)" /><Button disabled={busy || !cidr.trim()} onClick={add}>Add</Button></div></div>}{error && <p role="alert" className="text-xs text-destructive">{error}</p>}<div className="grid gap-1">{entries.length === 0 ? <p className="text-sm text-muted-foreground">No network restrictions.</p> : entries.map((entry) => <div key={entry.id} className="flex items-center gap-2 rounded-md border px-2 py-1.5 text-sm"><code className="flex-1">{entry.cidr}</code><span className="text-xs text-muted-foreground">{entry.label}</span>{owner && <Button variant="ghost" size="icon" className="size-7" aria-label={`Remove ${entry.cidr}`} disabled={busy} onClick={() => remove(entry.id)}><Trash2 /></Button>}</div>)}</div></section><EnterpriseControls workspace={workspace} /></DialogContent></Dialog><MembersDialog open={membersOpen} onOpenChange={setMembersOpen} workspace={workspace} currentUserId={currentUserId} /><AgentsDialog open={agentsOpen} onOpenChange={setAgentsOpen} workspace={workspace} /><WebhooksDialog open={webhooksOpen} onOpenChange={setWebhooksOpen} workspace={workspace} /></>;
}
